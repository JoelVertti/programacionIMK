#ifndef HEADER_FILE
#define HEADER_FILE

float perimetro(float a, float b);
float area(float a, float b);
float areaCirculo(float a);
float diametroCirculo(float a);
float circunferenciaCirculo(float a);
float Cm_Km(float Cm);
float Cm_M(float Cm);
double base, expo, power;
#endif

